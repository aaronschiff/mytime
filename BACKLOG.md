# Backlog

## Deferred features for future work

- **Auth & HTTPS:** If app is ever exposed beyond trusted LAN, add authentication (basic auth or OIDC) and enforce HTTPS. Revisited 2026-07-02 (installed Dock app shows "Not Secure" in the title bar) — user decided against it for now: no easy way to get a trusted cert for a `.local` LAN hostname without extra infra (Tailscale cert, own domain + DNS-01 + reverse proxy, or self-signed + manual per-device trust). Reconsider if Tailscale gets adopted for this LAN.
- **macOS menubar widget (Swift/SwiftUI) — done:** Native `MenuBarExtra` app mirroring the Today view — list today's timers, add entry, start/stop, click-to-edit elapsed time, full edit, delete; live-ticking elapsed in the menubar title; 4h still-running notification.
  - **Backend — done (2026-07-03):** `mytime/routers/api_today.py` — JSON API (`GET/POST/DELETE /api/today/*`), thin wrapper over the existing service layer, no new business logic. See README's "Menubar JSON API (backend)" section for the endpoint table and response-shape conventions. Design spec: `docs/superpowers/specs/2026-07-03-menubar-json-api-design.md` (gitignored, local only — not in git history).
  - **Frontend — done (2026-07-05):** SwiftUI `MenuBarExtra` app in `MyTimeMenuBar/` (repo root) — 6 files (`Models`, `APIClient`, `TimerStore`, `ContentView`, `SettingsView`, `MyTimeMenuBarApp`). `async/await`, `@Observable`, 1s display tick + 20s sync poll (full-state replace), 4h still-running notification. See README's "Menubar app (frontend)" section and `docs/superpowers/plans/2026-07-05-menubar-swiftui-app.md`.
  - **Deferred: keep a running entry running through Full Edit.** Editing a running entry currently stops it first (existing backend behavior in `edit_entry`), then applies the edit. User asked (2026-07-05) whether editing should instead keep the entry running, continuing from the edited time. Requires changing `mytime/routers/api_today.py`'s (or the underlying service's) `edit_entry` behavior — out of scope for the menubar-only plan that shipped the frontend above.